export type SEOProps = {
  title: string;
  ogTitle?: string;
  description: string;
  ogDescription?: string;
  name: string;
  type: string;
  url: string;
  previewImg: string;
  locale?: string;
};
