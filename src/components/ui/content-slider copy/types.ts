import { ReactNode } from "react";
import { TTabBarCotextValue } from "../../tab-bar copy/types";

export type TContentSliderUIProps = {
  tabBarContextValue: TTabBarCotextValue;
  children?: ReactNode;
};
